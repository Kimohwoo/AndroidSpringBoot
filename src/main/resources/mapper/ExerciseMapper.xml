<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.android.andriodproject.mapper.ExerciseMapper">
    
    <select id="GetData" resultType="com.android.andriodproject.DTO.UserDTO">
        select * from user where uid = #{uid}
    </select>
    
    <insert id="Insert">
    	insert into exercise(uid, fileName, filePath, exerciseTime, distance, calorie, dayNum)
        values(#{uid}, #{fileName}, #{filePath}, #{exerciseTime}, #{totalDistance}, #{calorie}, #{dayNum})
    </insert>
</mapper>